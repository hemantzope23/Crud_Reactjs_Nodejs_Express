import React from 'react';
import './myStylee.css'

function Stylesheets(props) {
    let className = p
    rops.primary ? 'primary' : ''
    return ( 
        <div >
        <h1 className = { `${className} font-xl` } > StyleSheets </h1> 

        </div>
    );
}
export default Stylesheets